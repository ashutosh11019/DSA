//                        null            exception throw
// Insert(head) -      offFirst(val)     addFirst(val)
// Remove(head) -      pollFirst()       removeFirst()
// Retrieval(head) -   peekFirst(),      getFirst()
// Insert(tail) -      offLast(val)      addFirst(val)
// Remove(tail) -      pollLast()        removeLast()
// Retrieva(tail)l -   peekLast(),       getLast()

package Queue;

// import java.util.ArrayDeque;

public class Array_Deque {
    public static void main(String[] args) {
        // ArrayDeque<Integer> aq = new ArrayDeque<>();
        // aq.addFirst(12);
        // aq.addFirst(24);
        // aq.pop();
        // System.out.println(aq.peek());
    }
}
